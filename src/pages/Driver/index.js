import { Fragment, Helmet } from "libraries";

const Driver = () => {

    return (
        <Fragment>
            <Helmet>
                <title>Shipper CMS - Driver Management</title>
            </Helmet>
        </Fragment>
    )
};

export default Driver;